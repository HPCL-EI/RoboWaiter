selector
    cond On(Coffee,Bar)
    sequence
        cond Holding(Coffee)
        selector
            sequence
                cond At(Robot,Bar)
                act PutDown(Coffee,Bar)
            act MoveTo(Bar)
