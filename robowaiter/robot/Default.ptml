selector{
    sequence{
        cond Chatting()
        act DealChat()
    }
    sequence{
        cond HasSubTask()
        sequence{
            act SubTaskPlaceHolder()
        }
        cond At(Talb,ea)
    }
}
