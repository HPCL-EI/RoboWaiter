selector
{
    sequence
    {
        cond Chatting()
        act DealChat()

    }
    sequence
    {
        cond HasSubTask()
        sequence
        {
            act SubTaskPlaceHolder()
        }
    }
    sequence
    {
        cond DetectCustomer()
        selector
        {
            cond At(Robot,Bar)
            act MoveTo(Bar)
        }
        act GreetCustomer()
    }
}